(ns bashketball-editor-api.github.cards-test
  "Legacy test namespace - see bashketball-editor-api.git.cards-test for current tests."
  (:require
   [clojure.test :refer [deftest is testing]]))

(deftest placeholder-test
  (testing "legacy namespace placeholder"
    (is true "See bashketball-editor-api.git.cards-test for card repository tests")))
