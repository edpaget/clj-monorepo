(ns bashketball-editor-api.github.sets-test
  "Legacy test namespace - see bashketball-editor-api.git.sets-test for current tests."
  (:require
   [clojure.test :refer [deftest is testing]]))

(deftest placeholder-test
  (testing "legacy namespace placeholder"
    (is true "See bashketball-editor-api.git.sets-test for set repository tests")))
